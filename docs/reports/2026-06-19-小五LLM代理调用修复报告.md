# 小五 LLM 代理调用修复报告

## 任务完成情况

- 已确认“小五创建 PRD”失败的核心原因是 Node 后端直连 OpenAI，没有走本机代理。
- 已新增 `undici` 依赖，用于在小五 LLM Provider 中显式配置 `ProxyAgent`。
- 小五 LLM Provider 现在会读取 `.env.local` 和进程环境中的 `HTTPS_PROXY`、`HTTP_PROXY`、`ALL_PROXY` 等代理变量。
- 前端 LLM 摘要已显示代理状态，例如“代理已启用”。
- 已重启本地 5173 服务并验证真实 PRD 生成成功。

## 验证结果

- 代理直连验证：通过 `http://127.0.0.1:7890` 请求 OpenAI `chat/completions` 返回 `200`。
- 小五 API 验证：`POST /api/xiaowu/prd` 返回 `ok: true`，生成 `SmallCalc PRD v1`。
- `npm test`：通过，5 个单元测试全部通过。
- `npm run lint`：通过。
- `npm run build`：通过。
- `npm run test:e2e`：通过，2 个 Playwright 测试全部通过。

## 自查与审计结论

- 本次没有添加 mock，也没有绕过真实 LLM。
- OpenAI 已通过代理真实调用成功。
- Azure OpenAI 配置仍保留为候选 provider，但当前主路径已由 OpenAI 成功返回。
- reset 后 `.agentbus` 只保留必要空文件，不保留测试产物。

## 术语影响

- 未新增核心术语。
- 确认“小五 LLM Provider”包含 provider 选择和代理连接能力。

## 代码注释检查

- 现有 `server/llm-provider.ts` 文件级注释仍准确：该模块负责读取环境配置并调用真实 LLM，不向前端泄露密钥。
- 本次新增代理逻辑较集中，未添加无意义逐行注释。

## 可能存在风险

- 小五后端是否能走代理，取决于启动 Vite / 后端进程时是否继承 `HTTPS_PROXY` 等环境变量，或 `.env.local` 中是否显式配置代理变量。
- 代理地址目前来自本机 `http://127.0.0.1:7890`；如果代理软件关闭，OpenAI 请求仍会失败。

## 需要请示的问题

- 是否要把 `HTTPS_PROXY=http://127.0.0.1:7890` 显式写入 `.env.local`，避免未来从不同入口启动时没有继承终端代理环境？
