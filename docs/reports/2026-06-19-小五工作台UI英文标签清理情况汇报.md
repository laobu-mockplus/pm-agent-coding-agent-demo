# 小五工作台 UI 英文标签清理情况汇报

## 任务完成情况

- 已去掉顶部 `PM Agent Workbench` 英文辅助标签。
- 已去掉左侧 `Replay` 英文辅助标签，只保留“7 步流程”。
- 已去掉中间 `Artifact Viewer` 英文辅助标签，只保留“产出物”。
- 已去掉右侧 `Conversation` 英文辅助标签，只保留“小五 / CC 会话”。
- 已将产物卡片中的 `Ready` / `Step` 改为中文“等待” / “第 N 步”。
- 已压缩顶部栏和面板标题区域，让 UI 更接近生产工具工作台，而不是演示占位界面。

## 验证结果

- `npm test`：通过，5 个单元测试全部通过。
- `npm run lint`：通过。
- `npm run build`：通过。
- `npm run test:e2e`：通过，2 个 Playwright 测试全部通过。
- 浏览器文本检查：`PM Agent Workbench`、`Replay`、`Artifact Viewer`、`Conversation` 均不再出现在页面文本中。

## 自查与审计结论

- 本次只调整 UI 呈现层，没有改动小五真实 LLM provider、agentbus、Codex App Server 调用器和 CC 执行机制。
- 页面 reset 后仍然不显示预置 PRD、TaskSpec、报告或验收结果。
- 桌面式固定窗口布局继续通过 E2E 验证。

## 术语影响

- 未新增核心术语。
- 继续确认“小五”“CC”“7 步流程”“产出物”“小五 / CC 会话”为当前 UI 的中文主术语。

## 代码注释检查

- 本次为展示层文案和间距调整，不涉及新的核心执行链路。
- 未新增不必要注释。

## 可能存在风险

- 如果后续继续引入 Codex、LLM、agentbus 等技术状态，需要控制英文技术词的展示边界，避免再次出现占位式英文标签。

## 需要请示的问题

- 无。
