# 小五工作台 UI 会话化修正情况汇报

生成时间：2026-06-19 20:25:00 CST  
任务：根据截图标注修正小五工作台 UI。

## 1. 任务完成情况

已完成。

本次 UI 修正包括：

- 删除顶部 `当前步骤 / 当前角色 / 流程进度 / 验收状态` 状态条。
- 中间区域从“步骤说明详情”改为“产出物查看器”。
- 中间区域只展示当前步骤产出物，例如 PRD、TaskSpec、ImplementationReport、验收报告等。
- 右侧从协议卡片列表改为“小五 / CC 会话”。
- Codex App Server 运行状态和结构化事件改为会话流中的实时消息。
- 保留左侧 7 步流程导航。
- 保持桌面式固定窗口布局，长内容仍在内部滚动容器中滚动。

## 2. 验证结果

自动化验证：

- `npm test`：通过，6 个单测全部通过。
- `npm run lint`：通过。
- `npm run build`：通过。
- `npm run test:e2e`：通过，2 个 Chromium e2e 测试全部通过。

浏览器真实检查：

- 页面地址：`http://127.0.0.1:5173/`
- `status-strip`：不存在。
- `artifact-viewer`：存在。
- `chat-list`：存在。
- `body.scrollHeight` 等于视口高度，没有页面级纵向滚动。
- 截图保存：`test-results/xiaowu-ui-chat-artifact.png`。

## 3. 自查与审计结论

通过。

修正后的主界面信息结构为：

```text
顶部：品牌 + 操作按钮
左侧：7 步流程
中间：产出物查看器
右侧：小五 / CC 会话
```

这与用户标注一致：删除顶部状态区，中间不再展示步骤说明，右侧不再展示协议调试卡片。

## 4. 术语影响

本次确认 UI 术语：

- `产出物`：中间主区域，用于查看 PRD、TaskSpec、报告、验收结论等。
- `小五 / CC 会话`：右侧通信区域，用于展示小五与 CC 的消息，以及 Codex App Server 的实时运行消息。

未新增 SmallCalc 产品术语。

## 5. 代码注释检查

本次主要是展示层结构调整，没有新增核心执行链路。已有文件级注释仍然适用。

## 6. 可能存在风险

- 当前右侧会话仍是基于 `.agentbus` 消息和 Codex events 的聚合展示，尚未提供用户输入框。
- 当前中间产出物内容来自流程定义和运行消息摘要，还没有做真实 Markdown/PDF/JSON 文件查看器。

## 7. 需要请示的问题

- 是否下一步把中间“产出物”改成真实文件查看器，直接读取 PRD、TaskSpec、ImplementationReport 的原始内容。
- 是否在右侧会话底部增加输入框，让老布或小五可以继续向 CC 追加消息。
