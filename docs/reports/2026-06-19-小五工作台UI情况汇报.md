# 小五工作台 UI 情况汇报

## 任务完成情况

已完成“小五 PM Agent 工作台 UI”demo 的真实通信探针。界面默认处于等待小五发令状态，SmallCalc 程序不会提前存在；用户点击开始后，先由小五创建 PRD，再由小五通过真实 `.agentbus/cc-inbox` 写入 `TaskSpec`，本地 orchestrator 拉起真实 `codex exec`，CC 执行后通过 `.agentbus/xiaowu-inbox` 写回 `ImplementationReport`。

## 验证结果

- `npm test`：通过，1 个测试文件、6 个测试用例全部通过。
- `npm run build`：通过，TypeScript 构建和 Vite 生产构建完成。
- `npm run lint`：通过。
- `npm run test:e2e`：通过，2 个 Chromium e2e 测试全部通过。
- 真实 Codex worker 探针：通过。runId 为 `run-1781868770264`，共记录 57 条执行事件，最终 exit code 为 `0`，CC 写回 `report-smallcalc-mvp-001.json`。

## 自查与审计结论

已按桌面式工作台方向设计界面：外层应用壳固定为 `100dvh`，主工作区使用内部滚动容器，避免页面级长滚动。自动化 e2e 测试已验证 `html` 和 `body` 的滚动高度不明显超过视口高度。

本次新增“通信通道”和“CC 执行台”视图，明确展示小五和 CC 的通信消息：`TaskSpec` 与 `ImplementationReport` 已接入真实文件通信；`ReviewRequest`、`FixReport` 和 `Approval` 仍属于后续流程展示。用户可以看到消息的发送方、接收方、通道、状态、payload，以及 CC stdout/stderr/exit 事件。

本次已清理 SmallCalc 提前实现痕迹：关闭旧的 SmallCalc TaskSpec Issue，关闭旧的 SmallCalc 实现 PR，删除远端实现分支，并清理本地 `.agentbus` 运行态记录。当前没有打开的 SmallCalc Issue 或 PR。

## 术语影响

本次确认使用以下术语：

- 小五：PM Agent 在 UI 中的产品角色名称。
- CC：Coding Agent 的简称。
- SmallCalc：本次 demo 的计算器 app 名称。
- TaskSpec：小五分配给 CC 的结构化任务。

本次未新增与既有文档冲突的术语。

## 代码注释检查

`src/App.tsx` 已补充文件级中文职责说明，明确当前阶段只模拟流程状态，不提前生成 SmallCalc 程序。后续接入真实 GitHub / Codex / Qoder 状态时，需要在数据适配层继续补充真实数据边界、触发边界和降级策略说明。

## 可能存在风险

| 风险 | 风险评级 | 建议是否立即解决 | 说明 |
| --- | --- | --- | --- |
| 当前 UI 使用固定 demo 数据，尚未实时读取 GitHub 或 Codex 运行状态 | 中 | 否 | 不影响本次可视化流程验收，但正式产品化必须接入真实状态源。 |
| 真实 CC 实现必须防止在小五发令前被提前触发 | 中 | 是 | 本次已把 UI 初始状态改为等待小五发令，并通过 `.agentbus/cc-inbox` 作为触发门禁；后续真实 SmallCalc 实现仍需沿用该门禁。 |
| 当前真实 Codex 探针只验证通信与报告，不实现 SmallCalc | 中 | 否 | 符合当前阶段目标；正式实现阶段需要把 TaskSpec 从通信探针升级为真实编码任务。 |
| 小五和 CC 当前仍可能共用同一 GitHub 账号 | 中 | 否 | 若后续重新启用真实 PR review，正式版本建议使用不同 GitHub App identity。 |
| 客户端安装与分发风险评估 | 低 | 否 | 本次仅改 Vite Web demo，未引入 Electron 打包、签名、公证、自动更新或本地文件迁移风险。 |

## 需要请示的问题

暂无必须立即请示的问题。后续若进入真实编排 UI，需要确认数据源优先级：从 GitHub 读取、从本地 `.agentbus` 读取，还是由独立 orchestrator 提供统一 API。
