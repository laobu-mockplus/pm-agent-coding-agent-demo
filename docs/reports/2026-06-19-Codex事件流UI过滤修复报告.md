# Codex 事件流 UI 过滤修复报告

## 任务完成情况

- 已过滤右侧“小五 / CC 会话”中的高频 Codex App Server delta 事件。
- `item/agentMessage/delta` 不再显示在 UI 会话列表中。
- `item/reasoning/delta` 和 `thread/tokenUsage/updated` 也不再显示。
- 命令执行输出 `item/commandExecution/outputDelta` 仍保留，因为它属于测试、构建和脚本运行证据。
- 原始事件仍保留在 `.agentbus/runs/<runId>/events.jsonl`，不影响审计。

## 验证结果

- `npm test`：通过，5 个单元测试全部通过。
- `npm run lint`：通过。
- `npm run build`：通过。
- `npm run test:e2e`：通过，2 个 Playwright 测试全部通过。
- 浏览器检查：右侧会话文本中不再包含 `item/agentMessage/delta` 和 `thread/tokenUsage/updated`。

## 自查与审计结论

- 本次只修改 UI 展示过滤，不修改 Codex App Server 原始事件记录。
- 过滤规则保留命令执行类事件，避免影响后续测试证据展示。
- 小五 / CC 业务消息、TaskSpec、ImplementationReport 仍正常展示。

## 术语影响

- 未新增核心术语。
- 明确“Codex delta 事件”属于运行噪音，不作为默认会话消息展示。

## 代码注释检查

- 本次新增 `shouldShowCodexEvent` 展示过滤函数，逻辑简单清晰。
- 未新增不必要注释。

## 可能存在风险

- 如果未来需要在 UI 中调试完整 token 级输出，需要新增“调试模式”或“原始事件面板”，而不是恢复到默认会话流中。

## 需要请示的问题

- 是否需要在小五工作台增加一个折叠的“Codex 原始事件”调试区？
