# 小五工作台去 Mock 与真实 LLM 接入情况汇报

## 任务完成情况

- 已移除主流程中“点击步骤即出现结果”的预置数据逻辑。
- 左侧 7 步流程现在只负责查看状态，不再制造 PRD、TaskSpec、报告或验收结论。
- 新增小五 LLM Provider，读取父级工作区 `.env.local` 中的 `OPENAI_*` 与 `AZURE_OPENAI_*` 配置。
- 小五创建 PRD、生成 TaskSpec、生成验收报告均改为真实 LLM API 调用。
- TaskSpec 仍通过 `.agentbus/cc-inbox/` 写入，并由 CC 调用器通过 Codex App Server 启动 CC。
- UI 中间区域只显示真实产物；右侧显示小五 / CC 会话与 Codex App Server 事件。
- 真实 LLM 调用失败时，API 返回结构化错误，UI 展示错误，不生成假产物。

## 验证结果

- `npm test`：通过，5 个单元测试全部通过。
- `npm run lint`：通过。
- `npm run build`：通过。
- `npm run test:e2e`：通过，2 个 Playwright 测试全部通过。
- E2E 覆盖了两种情况：
  - 页面初始没有任何预置结果。
  - 真实 LLM provider 不可用时，页面显示 provider 错误且不生成 PRD。

本机真实 provider 探测结果：

- OpenAI endpoint：`api.openai.com:443` 连接超时，错误码 `UND_ERR_CONNECT_TIMEOUT`。
- Azure OpenAI endpoint：网络可达，但当前 `.env.local` 中的 key 或 endpoint 组合返回 `401`。

因此，本次在当前机器环境下没有真实生成出 PRD；这是外部 provider 可用性阻塞，不是系统 fallback 到 mock。

## 自查与审计结论

- 已确认 `.agentbus/artifacts/` 是小五真实产物存储位置。
- 已确认 `.agentbus/cc-inbox/` 只有在 PRD 真实存在后才会写入 TaskSpec。
- 已确认 `.agentbus/xiaowu-inbox/` 只有 CC 写回 ImplementationReport 后才会产生报告。
- 已确认 reset 会清空 artifacts、cc-inbox、xiaowu-inbox、runs，避免历史结果污染下一次测试。
- 已确认 SmallCalc 程序没有被提前实现。

## 术语影响

- 确认“小五”作为 PM Agent 名称继续沿用。
- 确认“CC 调用器”作为 Coding Agent adapter 层概念继续沿用。
- 新增并固定“小五 LLM Provider”概念：负责读取 `.env.local` 并执行真实 LLM 调用，不负责生成 mock 内容。

## 代码注释检查

- `server/llm-provider.ts` 已补充职责说明，明确密钥只在后端使用，前端只展示非敏感摘要。
- `src/App.tsx` 保留小五工作台入口注释，说明小五先发 TaskSpec，再由 CC 调用器启动真实 Coding Agent。
- 本次未添加逐行解释型噪音注释。

## 可能存在风险

- 当前 `.env.local` 的 OpenAI 网络不可达，Azure OpenAI 返回 401；需要修正网络、代理、key 或 endpoint 后，才能在本机真实生成 PRD。
- E2E 现在允许真实 provider 不可用时通过“错误路径”验证，目的是防止误造假数据；它不能替代一次 provider 可用时的完整真实链路验收。
- 当前 TaskSpec 仍要求 CC 本轮只读取任务并写回报告，不实现 SmallCalc；后续要验证完整 7 步，需要让小五在第一次验收后真实下发修复 / 实现任务。

## 需要请示的问题

- 是否优先修正 `.env.local` 中 OpenAI / Azure OpenAI 的可用性，以便完成一次真实 PRD 与 TaskSpec 生成验收？
- 后续完整 7 步流程是否要直接进入“CC 真实实现 SmallCalc 并提交报告”的阶段？
